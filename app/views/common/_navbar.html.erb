<!DOCTYPE html>
<html>
  <head>
    <style>
    .top-nav {
      font-family: sans-serif;
      position: fixed;
      top: 0;
      z-index: 5680;
      width: 120%;
      background-color: #e5e5e5;
      min-height: 60px;
    }
    </style>
  </head>

  <nav class="navbar navbar-default navbar-fixed-top" role="navigation" id="navbar">
    <div class="container-fluid">
       <!-- Brand and toggle get grouped for better mobile display -->
       <div class="navbar-header">
         <!-- <img src='<%= image_url 'BSR logo.PNG' %>' > -->
         <a class="navbar-brand" style="color:white; font-family:helvetica; font-size:16px" href="/"><i class="fa fa-superpowers"></i> BosGalRun</a>
       </div>

       <!-- Collect the nav links, forms, and other content for toggling -->
       <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
           <li>
             <%= link_to("夏季课程", products_path) %>
           </li>
           <li>
             <%= link_to("周末元气跑", products_path) %>
           </li>
           <li>
             <%= link_to("55天马甲线计划", products_path) %>
           </li>
           <li>
             <%= link_to("关于BosGalRun", products_path) %>
           </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
           <li>
             <%= link_to carts_path do %>
                购物车 <i class="fa fa-shopping-cart"> </i> ( <%= current_cart.products.count %>)
             <% end %>
           </li>
           <!--=== 帐号操作 ===-->
           <% if !current_user %>
           <li><%= button_to("登录", new_user_session_path, method: :get, class: "btn btn-default navbar-btn") %></li>
           <li><%= button_to("注册", new_user_registration_path, method: :get, class: "btn btn-default navbar-btn") %> </li>         <% else %>

             <li class="dropdown">
               <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                   Hi!, <%= current_user.email %>
                   <b class="caret"></b>
               </a>
               <ul class="dropdown-menu">
                 <% if current_user.admin? %>
                   <li>
                    <%= link_to("后台管理", admin_products_path ) %>
                   </li>

                 <% end %>
                 <li class="divider"> </li>
                   <li> <%= link_to("我的订单", account_orders_path )%> </li>
                   <li><%= link_to(content_tag(:i, "我的收藏", class: "fa fa-heart fa-md"), products_path(:favorite => "yes"))%> </li>
                 <li class="divider"> </li>
                   <li> <%= link_to(content_tag(:i, '登出', class: 'fa fa-sign-out'), destroy_user_session_path, method: :delete) %> </li>
               </ul>
             </li>
           <% end %>
         </ul>
       </div>
       <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
  </nav>

</html>
